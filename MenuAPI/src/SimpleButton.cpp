#include "SimpleButton.h

 SimpelButton::SimpleButton(ofVec2f loc, ofTexture& norm, ofTexture& hov, ofTrueTypeFont* f);
{
    location = loc;
    normal = norm;
    hover = hov;
    font = f;
}

void update(ofVec2f& mousePos)
{
    if(pointisin)
    {

    }
}
